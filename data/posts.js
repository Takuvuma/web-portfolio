/* ------------------------------------------------------------------
   Blog index. One object per post — newest first (order doesn't matter,
   the page sorts by date).

     slug     file name under posts/ without .html
     title    post title
     date     "YYYY-MM-DD"
     summary  one or two sentences shown on the card
     tags     array of short labels
     minutes  estimated read time

   Run ./new-post.sh "Your title here" to scaffold a post and add its
   entry here automatically.
------------------------------------------------------------------- */

window.IAM_POSTS = [
  // ← posts go here
];
